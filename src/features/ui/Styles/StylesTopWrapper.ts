import { css } from "styled-components";

const StylesTopWrapper = css`
  background: white;
  box-shadow: 0 0.125rem 0.25rem rgb(0 0 0 / 8%);
  padding: 10px 0;
`;

export default StylesTopWrapper;

// calling this "topwrapper" cause i'm using it on both nav and header and these are usually located at the top of the page
